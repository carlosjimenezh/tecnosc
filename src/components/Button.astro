---
interface Props {
  customClass?: string;
}
const { customClass = '' } = Astro.props;
---

<button class={`bg-[#ff4b5c] text-white font-semibold px-6 py-2 rounded-lg shadow-lg transition-colors duration-300 hover:bg-[#ff6b81] focus:outline-none cursor-pointer ${customClass}`}>
  <slot />
</button>

